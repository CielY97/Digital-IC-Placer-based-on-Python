# main.py

from main_engine import PlacementEngine

if __name__ == "__main__":
    # =====================================================================
    # 核心配置字典 (Configuration Dictionary)
    # 你可以在这里随心所欲地调整芯片的规模和算法的参数！
    # =====================================================================
    config = {
        # 1. 物理画布与网表参数
        "num_cells": 500,        # 生成的门单元数量 (试试改到 1000 看看有多壮观)
        "num_nets": 600,         # 生成的线网数量
        "design_w": 200.0,       # 芯片画布宽度
        "design_h": 200.0,       # 芯片画布高度
        "cell_w": 2.5,           # 默认标准单元宽度
        "cell_h": 10.0,          # 默认标准单元高度 (通常也是 Row 的高度)
        "num_clusters": 5,       # 将电路逻辑分为几个聚簇 (影响视觉上的星团数量)

        # 2. 模拟退火 (SA) 全局布局参数
        "sa_T0": 1000.0,         # 初始温度 (越高，前期的乱动幅度越大)
        "sa_T_end": 1.0,         # 终止温度 (越低，后期的收敛越精细)
        "sa_alpha": 0.88,        # 降温系数 (0.8 ~ 0.99 之间，越接近1降温越慢，效果越好但时间越长)
        "sa_inner_iter": 3000,   # 每个温度下的迭代次数 (数据规模大时，需同步调大此参数)
    }

    # =====================================================================
    # 实例化引擎并一键运行
    # =====================================================================
    engine = PlacementEngine(config)
    engine.run_flow()